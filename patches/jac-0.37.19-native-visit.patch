--- a/jaclang/compiler/backends/native/na_ir_gen_pass.jac
+++ b/jaclang/compiler/backends/native/na_ir_gen_pass.jac
@@ -2427,3 +2427,5 @@
     def _lower_view_container(t: any) -> (ir.Type | None);
     def _program_has_subclasses(arch_name: str) -> bool;
 }
+
+# QuakePassion native visit ownership patch.
--- a/jaclang/compiler/backends/native/na_ir_gen_pass.impl/osp.impl.jac
+++ b/jaclang/compiler/backends/native/na_ir_gen_pass.impl/osp.impl.jac
@@ -1512,18 +1512,22 @@
         }
     }
     _argtys = osp_visit_fn.function_type.args;
+    # Coercing a list may create blocks and replace the active builder.
+    # Evaluate arguments before capturing the builder for the call.
+    _visit_targets = self._coerce_type(_targets, _argtys[0]);
+    _visit_insert = self._coerce_type(_insert, _argtys[1]);
     _did = self.builder.call(
         osp_visit_fn,
-        [
-            self._coerce_type(_targets, _argtys[0]),
-            self._coerce_type(_insert, _argtys[1])
-        ],
+        [_visit_targets, _visit_insert],
         name="visit.did"
     );
 
-    if isinstance(_targets.type, ir.PointerType) and self._is_owned(_targets) {
+    # List reflavoring consumes an owned source and returns an owned copy.
+    # Release that copy, not the source that coercion has already released.
+    _release_targets = _visit_targets if self._is_owned(_visit_targets) else _targets;
+    if isinstance(_release_targets.type, ir.PointerType) and self._is_owned(_release_targets) {
         self._emit_rc_release_simple(
-            _targets, op="visit_targets", loc=getattr(_targets, 'jac_loc', '')
+            _release_targets, op="visit_targets", loc=getattr(_targets, 'jac_loc', '')
         );
     }
     if nd.else_body is None {
