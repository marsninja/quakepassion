diff --git a/jaclang/compiler/backends/native/impl/primitives_native.impl.jac b/jaclang/compiler/backends/native/impl/primitives_native.impl.jac
index 938d5234bc..4d361bb1c5 100644
--- a/jaclang/compiler/backends/native/impl/primitives_native.impl.jac
+++ b/jaclang/compiler/backends/native/impl/primitives_native.impl.jac
@@ -4991,40 +4991,23 @@ impl NativeBuiltinEmitter.emit_sorted(
         return None;
     }
     p = ctx.pass_ref;
-    list_val = args[0];
-
-    helpers: (dict[str, any] | None) = None;
-    elem_key: (str | None) = None;
-    for (lk, lt) in p.list_types.items() {
-        if list_val.type == lt.as_pointer() {
-            helpers = p.list_helpers.get(lk);
-            elem_key = lk;
-            break;
-        }
-    }
-    if helpers is None {
-        return None;
-    }
+    items = self.emit_list(ctx, args);
+    elem_key = p._infer_list_elem_type(items) if items is not None else None;
     _le = p._native_emitters.get("list") if p?._native_emitters else None;
-    if not isinstance(_le, NativeListEmitter) {
+    if items is None or elem_key is None or not isinstance(_le, NativeListEmitter) {
         return None;
     }
     _ctx2 = ctx.__class__(
         pass_ref=p,
-        type_key=elem_key or "",
+        type_key=elem_key,
         call_kwargs=(ctx?.call_kwargs or {}),
         arg_nodes=ctx.arg_nodes,
         call_node=ctx.call_node
     );
-
-    sorted_copy = _le.emit_copy(_ctx2, list_val, []);
-    if sorted_copy is None {
+    if _le.emit_sort(_ctx2, items, []) is None and (ctx?.call_kwargs or {}) {
         return None;
     }
-    if _le.emit_sort(_ctx2, sorted_copy, []) is None and (ctx?.call_kwargs or {}) {
-        return None;
-    }
-    return sorted_copy;
+    return items;
 }
 
 impl NativeBuiltinEmitter.emit_reversed(
@@ -5612,36 +5595,7 @@ impl NativeBuiltinEmitter.emit_list(
         if elem_type is None {
             return None;
         }
-        list_type = p._lower_list_for_elem(elem_type);
-        spec = p._spec_from_container_ptr(list_type);
-        if spec is None {
-            return None;
-        }
-        helpers = p.list_helpers.get(spec);
-        if helpers is None {
-            return None;
-        }
-        slot_type = ir.IntType(8).as_pointer()
-            if elem_type == p._str_desc_ty
-            else elem_type;
-        slot = p._entry_alloca(slot_type, "list.iter.element");
-        result = b.call(helpers["new"], [], name="list.iter.result");
-        func = b.basic_block.function;
-        condition = func.append_basic_block("list.iter.next");
-        body = func.append_basic_block("list.iter.append");
-        done = func.append_basic_block("list.iter.done");
-        b.branch(condition);
-        b.position_at_end(condition);
-        available = p._emit_iter_advance(b, val, slot);
-        b.cbranch(available, body, done);
-        b.position_at_end(body);
-        stored_type = helpers["elem_type"];
-        assert isinstance(stored_type, ir.Type);
-        element = p._coerce_type(b.load(slot), stored_type);
-        b.call(helpers["append"], [result, element]);
-        b.branch(condition);
-        b.position_at_end(done);
-        return result;
+        return p._list_from_iter(val, elem_type, elem_type);
     }
 
     if p._is_bytes_value(val) {
@@ -5729,7 +5683,19 @@ impl NativeBuiltinEmitter.emit_list(
             return _le3.emit_copy(_ctx3, _lv, []);
         }
     }
-    return None;
+    if source is None {
+        return None;
+    }
+    built = p._make_iter_from_value(p._str_norm(val), source);
+    if built is None {
+        return None;
+    }
+    (iter_val, _elem_name, elem_type) = built;
+    drained = p._list_from_iter(
+        iter_val, elem_type, p._list_elem_llvm_of(ctx.call_node) or elem_type
+    );
+    p._emit_rc_release_simple(iter_val, op="list_iter", loc="");
+    return drained;
 }
 
 impl NativeBuiltinEmitter.emit_dict(
diff --git a/jaclang/compiler/backends/native/na_ir_gen_pass.impl/iterators.impl.jac b/jaclang/compiler/backends/native/na_ir_gen_pass.impl/iterators.impl.jac
index 96134a5a3a..77eea99adc 100644
--- a/jaclang/compiler/backends/native/na_ir_gen_pass.impl/iterators.impl.jac
+++ b/jaclang/compiler/backends/native/na_ir_gen_pass.impl/iterators.impl.jac
@@ -549,9 +549,40 @@ impl NaIRGenPass._make_range_iter(range_val: ir.Value) -> ir.Value {
     return self._make_jac_iter(next_fn, state);
 }
 
+impl NaIRGenPass._list_from_iter(
+    iter_val: ir.Value, iter_elem_type: ir.Type, list_elem_type: ir.Type
+) -> (ir.Value | None) {
+    spec = self._spec_from_container_ptr(self._lower_list_for_elem(list_elem_type));
+    helpers = self.list_helpers.get(spec) if spec is not None else None;
+    if helpers is None {
+        return None;
+    }
+    b = self.builder;
+    slot_type = ir.IntType(8).as_pointer()
+        if iter_elem_type == self._str_desc_ty
+        else iter_elem_type;
+    slot = self._entry_alloca(slot_type, "list.iter.element");
+    result = b.call(helpers["new"], [], name="list.iter.result");
+    func = b.basic_block.function;
+    condition = func.append_basic_block("list.iter.next");
+    body = func.append_basic_block("list.iter.append");
+    done = func.append_basic_block("list.iter.done");
+    b.branch(condition);
+    b.position_at_end(condition);
+    b.cbranch(self._emit_iter_advance(b, iter_val, slot), body, done);
+    b.position_at_end(body);
+    stored_type = helpers["elem_type"];
+    assert isinstance(stored_type, ir.Type);
+    b.call(helpers["append"], [result, self._coerce_type(b.load(slot), stored_type)]);
+    b.branch(condition);
+    b.position_at_end(done);
+    return result;
+}
+
 impl NaIRGenPass._make_iter_from_value(
     val: ir.Value, expr: uni.UniNode
 ) -> (tuple[ir.Value, str, ir.Type] | None) {
+    self._ensure_iter_types();
     i8p = ir.IntType(8).as_pointer();
 
     _checked_list = self._list_spec_of(expr);
diff --git a/jaclang/compiler/backends/native/na_ir_gen_pass.jac b/jaclang/compiler/backends/native/na_ir_gen_pass.jac
index 1ac1146e90..80902a8747 100644
--- a/jaclang/compiler/backends/native/na_ir_gen_pass.jac
+++ b/jaclang/compiler/backends/native/na_ir_gen_pass.jac
@@ -1432,6 +1432,10 @@ walker NaIRGenPass {
 
     def _range_iter_next_fn -> ir.Function;
     def _make_range_iter(range_val: ir.Value) -> ir.Value;
+    def _list_from_iter(
+        iter_val: ir.Value, iter_elem_type: ir.Type, list_elem_type: ir.Type
+    ) -> (ir.Value | None);
+
     def _make_iter_from_value(
         val: ir.Value, expr: uni.UniNode
     ) -> (tuple[ir.Value, str, ir.Type] | None);
